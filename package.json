
{
  "name": "huffman-c-backend",
  "version": "1.0.0",
  "description": "Node.js Express server to act as a bridge for a C-based Huffman compression binary.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "multer": "^1.4.5-lts.1"
  }
}
